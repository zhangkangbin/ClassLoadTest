apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:3.1.0'
}

repositories {
    mavenCentral()
}

group='com.g'
version='1.2.0'

uploadArchives{
    repositories {
        mavenDeployer {
            repository(url: uri('F:\\code\\ClassLoadTest\\repo'))
        }
    }
}